<ui:FluentWindow x:Class="ABLyrics.App.Views.StyleSettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        Title="样式设置"
        Height="640"
        Width="780"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- 左侧导航：220 宽的 NavigationView -->
        <ui:NavigationView x:Name="NavView"
                           Grid.Row="0" Grid.Column="0"
                           Width="220"
                           IsBackButtonVisible="Collapsed"
                           PaneDisplayMode="Left"
                           OpenPaneLength="220">
            <ui:NavigationView.MenuItems>
                <!--
                  注意：不能用 NavigationView.ItemInvoked + NavView.SelectedItem 切页。
                  原因：WPF-UI 3.1.x 的 NavigationViewItem.OnClick() 里只有在
                        TargetPageType 不为 null 时才会调用 OnNavigationViewItemClick，
                        而我们这里只设了 TargetPageTag，没有 page 类，导致
                        SelectedItem 永远不被同步更新 → handler 看到 NavView.SelectedItem == null
                        → 所有 ScrollViewer 都被折叠 → 用户看到空白，菜单"失效"。
                  修复：直接订阅每个 NavigationViewItem 的 Click 事件，由 OnMenuItemClick
                        处理 TargetPageTag -> ScrollViewer 路由。
                -->
                <ui:NavigationViewItem Content="外观" TargetPageTag="appearance" Click="OnMenuItemClick" />
                <ui:NavigationViewItem Content="布局" TargetPageTag="layout" Click="OnMenuItemClick" />
                <ui:NavigationViewItem Content="颜色" TargetPageTag="color" Click="OnMenuItemClick" />
                <ui:NavigationViewItem Content="同步" TargetPageTag="sync" Click="OnMenuItemClick" />
                <ui:NavigationViewItem Content="关于" TargetPageTag="about" Click="OnMenuItemClick" />
            </ui:NavigationView.MenuItems>
        </ui:NavigationView>

        <!-- 右侧 5 个页面（Column 1，与 NavigationView 不重叠） -->
        <Grid Grid.Row="0" Grid.Column="1">

            <!-- 外观页 -->
            <ScrollViewer x:Name="AppearancePage" Visibility="Visible"
                          VerticalScrollBarVisibility="Auto" Padding="24">
                <StackPanel>
                    <TextBlock Text="字体与排版" FontSize="20" FontWeight="SemiBold" Margin="0,0,0,16" />
                    <ui:Card Padding="20" Margin="0,0,0,16">
                        <StackPanel>
                            <TextBlock Text="字体" Margin="0,0,0,8" />
                            <ComboBox x:Name="FontFamilyCombo"
                                      Height="32"
                                      Margin="0,0,0,16"
                                      MaxDropDownHeight="280"
                                      IsEditable="True"
                                      IsTextSearchEnabled="True"
                                      VirtualizingStackPanel.IsVirtualizing="True"
                                      VirtualizingStackPanel.VirtualizationMode="Recycling"
                                      PreviewMouseLeftButtonDown="OnFontComboPreviewMouseLeftButtonDown"
                                      PreviewKeyDown="OnFontComboPreviewKeyDown" />

                            <TextBlock Text="字号" Margin="0,0,0,8" />
                            <DockPanel Margin="0,0,0,16">
                                <TextBlock x:Name="FontSizeLabel"
                                           DockPanel.Dock="Right"
                                           Width="40"
                                           VerticalAlignment="Center"
                                           HorizontalAlignment="Right" />
                                <Slider x:Name="FontSizeSlider"
                                        Minimum="12"
                                        Maximum="36"
                                        TickFrequency="1"
                                        IsSnapToTickEnabled="True" />
                            </DockPanel>

                            <TextBlock Text="字距 (px)" Margin="0,0,0,8" />
                            <DockPanel>
                                <TextBlock x:Name="LetterSpacingLabel"
                                           DockPanel.Dock="Right"
                                           Width="40"
                                           VerticalAlignment="Center"
                                           HorizontalAlignment="Right" />
                                <Slider x:Name="LetterSpacingSlider"
                                        Minimum="-2"
                                        Maximum="20"
                                        TickFrequency="0.5"
                                        IsSnapToTickEnabled="True" />
                            </DockPanel>
                        </StackPanel>
                    </ui:Card>

                    <ui:Card Padding="20">
                        <StackPanel>
                            <TextBlock Text="预览" FontWeight="SemiBold" Margin="0,0,0,12" />
                            <Border x:Name="PreviewBorder"
                                    CornerRadius="6"
                                    Padding="12,6">
                                <StackPanel>
                                    <TextBlock x:Name="PreviewSecondary"
                                               Text="上一行歌词预览"
                                               TextAlignment="Center"
                                               Margin="0,0,0,2" />
                                    <TextBlock x:Name="PreviewPrimary"
                                               Text="当前行歌词预览"
                                               TextAlignment="Center" />
                                </StackPanel>
                            </Border>
                        </StackPanel>
                    </ui:Card>
                </StackPanel>
            </ScrollViewer>

            <!-- 布局页 -->
            <ScrollViewer x:Name="LayoutPage" Visibility="Collapsed"
                          VerticalScrollBarVisibility="Auto" Padding="24">
                <StackPanel>
                    <TextBlock Text="窗口布局" FontSize="20" FontWeight="SemiBold" Margin="0,0,0,16" />
                    <ui:Card Padding="20" Margin="0,0,0,16">
                        <StackPanel>
                            <TextBlock Text="框高度 (px)" Margin="0,0,0,8" />
                            <DockPanel>
                                <TextBlock x:Name="BarHeightLabel"
                                           DockPanel.Dock="Right"
                                           Width="40"
                                           VerticalAlignment="Center" />
                                <Slider x:Name="BarHeightSlider"
                                        Minimum="28"
                                        Maximum="120"
                                        TickFrequency="1"
                                        IsSnapToTickEnabled="True" />
                            </DockPanel>
                        </StackPanel>
                    </ui:Card>

                    <ui:Card Padding="20" Margin="0,0,0,16">
                        <StackPanel>
                            <TextBlock Text="四周边距 (px)" FontWeight="SemiBold" Margin="0,0,0,12" />
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>
                                <StackPanel Grid.Row="0" Grid.Column="0" Margin="0,0,8,12">
                                    <TextBlock Text="左" FontSize="11" Opacity="0.7" Margin="0,0,0,4" />
                                    <ui:TextBox x:Name="PaddingLeftBox" />
                                </StackPanel>
                                <StackPanel Grid.Row="0" Grid.Column="1" Margin="8,0,0,12">
                                    <TextBlock Text="上" FontSize="11" Opacity="0.7" Margin="0,0,0,4" />
                                    <ui:TextBox x:Name="PaddingTopBox" />
                                </StackPanel>
                                <StackPanel Grid.Row="1" Grid.Column="0" Margin="0,0,8,0">
                                    <TextBlock Text="右" FontSize="11" Opacity="0.7" Margin="0,0,0,4" />
                                    <ui:TextBox x:Name="PaddingRightBox" />
                                </StackPanel>
                                <StackPanel Grid.Row="1" Grid.Column="1" Margin="8,0,0,0">
                                    <TextBlock Text="下" FontSize="11" Opacity="0.7" Margin="0,0,0,4" />
                                    <ui:TextBox x:Name="PaddingBottomBox" />
                                </StackPanel>
                            </Grid>
                        </StackPanel>
                    </ui:Card>

                    <ui:Card Padding="20">
                        <StackPanel>
                            <TextBlock Text="显示行数" Margin="0,0,0,12" />
                            <StackPanel Orientation="Horizontal">
                                <RadioButton x:Name="OneLineRadio"
                                             Content="1 行"
                                             Margin="0,0,24,0"
                                             GroupName="LineCount" />
                                <RadioButton x:Name="TwoLineRadio"
                                             Content="2 行"
                                             GroupName="LineCount" />
                            </StackPanel>
                        </StackPanel>
                    </ui:Card>
                </StackPanel>
            </ScrollViewer>

            <!-- 颜色页 -->
            <ScrollViewer x:Name="ColorPage" Visibility="Collapsed"
                          VerticalScrollBarVisibility="Auto" Padding="24">
                <StackPanel>
                    <TextBlock Text="颜色与背景" FontSize="20" FontWeight="SemiBold" Margin="0,0,0,16" />
                    <ui:Card Padding="20" Margin="0,0,0,16">
                        <StackPanel>
                            <TextBlock Text="底色 (RGB Hex)" Margin="0,0,0,8" />
                            <ui:TextBox x:Name="BackgroundColorBox" />
                            <TextBlock Text="例如 #101010" FontSize="11" Opacity="0.6" Margin="0,6,0,0" />
                        </StackPanel>
                    </ui:Card>
                    <ui:Card Padding="20">
                        <StackPanel>
                            <TextBlock Text="背景透明度" Margin="0,0,0,8" />
                            <DockPanel>
                                <TextBlock x:Name="OpacityLabel"
                                           DockPanel.Dock="Right"
                                           Width="48"
                                           VerticalAlignment="Center" />
                                <Slider x:Name="OpacitySlider"
                                        Minimum="0"
                                        Maximum="1"
                                        TickFrequency="0.05" />
                            </DockPanel>
                        </StackPanel>
                    </ui:Card>
                </StackPanel>
            </ScrollViewer>

            <!-- 同步页 -->
            <ScrollViewer x:Name="SyncPage" Visibility="Collapsed"
                          VerticalScrollBarVisibility="Auto" Padding="24">
                <StackPanel>
                    <TextBlock Text="歌词同步" FontSize="20" FontWeight="SemiBold" Margin="0,0,0,16" />
                    <ui:Card Padding="20">
                        <StackPanel>
                            <TextBlock Text="同步偏移 (ms)" Margin="0,0,0,8" />
                            <DockPanel Margin="0,0,0,12">
                                <TextBlock x:Name="SyncOffsetLabel"
                                           DockPanel.Dock="Right"
                                           Width="40"
                                           VerticalAlignment="Center"
                                           HorizontalAlignment="Right" />
                                <Slider x:Name="SyncOffsetSlider"
                                        Minimum="0"
                                        Maximum="500"
                                        TickFrequency="10"
                                        IsSnapToTickEnabled="True" />
                            </DockPanel>
                            <TextBlock TextWrapping="Wrap" Opacity="0.7" FontSize="12"
                                       Text="正值让歌词延后显示，负值让歌词提前显示。如果歌词总是慢/快半拍，可以微调此值。" />
                        </StackPanel>
                    </ui:Card>
                </StackPanel>
            </ScrollViewer>

            <!-- 关于页 -->
            <ScrollViewer x:Name="AboutPage" Visibility="Collapsed"
                          VerticalScrollBarVisibility="Auto" Padding="24">
                <StackPanel>
                    <TextBlock Text="关于" FontSize="20" FontWeight="SemiBold" Margin="0,0,0,16" />
                    <ui:Card Padding="20">
                        <StackPanel>
                            <TextBlock Text="ABLyrics" FontSize="24" FontWeight="SemiBold" />
                            <TextBlock Text="Windows 桌面歌词小组件" Opacity="0.7" Margin="0,4,0,16" />
                            <TextBlock Text="技术栈" Opacity="0.6" FontSize="12" Margin="0,0,0,4" />
                            <TextBlock Text=".NET 8 + WPF + WPF-UI (lepoco)" Margin="0,0,0,12" />
                            <TextBlock Text="歌词源" Opacity="0.6" FontSize="12" Margin="0,0,0,4" />
                            <TextBlock Text="LRCLIB · Netease · 本地 .lrc" Margin="0,0,0,12" />
                            <TextBlock Text="许可" Opacity="0.6" FontSize="12" Margin="0,0,0,4" />
                            <TextBlock Text="MIT" />
                        </StackPanel>
                    </ui:Card>
                </StackPanel>
            </ScrollViewer>
        </Grid>

        <!-- 底部按钮行（跨两列） -->
        <Border Grid.Row="1" Grid.ColumnSpan="2" Padding="20,12"
                Background="{DynamicResource ControlFillColorDefaultBrush}">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <ui:Button Content="取消"
                           Width="84"
                           Margin="0,0,8,0"
                           Click="OnCancelClick" />
                <ui:Button Content="应用"
                           Width="84"
                           Margin="0,0,8,0"
                           Click="OnApplyClick" />
                <ui:Button Content="确定"
                           Width="84"
                           Appearance="Primary"
                           Click="OnConfirmClick" />
            </StackPanel>
        </Border>
    </Grid>
</ui:FluentWindow>
