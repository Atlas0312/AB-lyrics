<UserControl x:Class="ABLyrics.App.Views.CandidateColumnView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             x:Name="Root"
             Cursor="Hand"
             MouseLeftButtonUp="OnRootClicked">
    <Border x:Name="CardBorder" BorderBrush="Gray" BorderThickness="1" Padding="12" Margin="4"
            Background="Transparent">
        <StackPanel>
            <DockPanel LastChildFill="True" Margin="0,0,0,8">
                <Border DockPanel.Dock="Right"
                        x:Name="SelectedBadge"
                        Background="#2196F3" CornerRadius="3" Padding="6,1" Margin="6,0,0,0"
                        Visibility="Collapsed">
                    <TextBlock Text="已选" Foreground="White" FontSize="11"/>
                </Border>
                <TextBlock x:Name="HeaderText" FontWeight="SemiBold" VerticalAlignment="Center"/>
            </DockPanel>
            <TextBlock x:Name="PreviousLineText" Opacity="0.5" Margin="0,4"/>
            <TextBlock x:Name="CurrentLineText" FontSize="18" FontWeight="Bold" Margin="0,4"/>
            <TextBlock x:Name="NextLineText" Opacity="0.5" Margin="0,4"/>
        </StackPanel>
    </Border>
</UserControl>
